# To-do list

## functions and methods

- `zip()`
- `list()`
- `math.ceil()`
- `math.floor()`
- `setdefault()`
- `keys()`
- `values()`
- `add()`
- `pop()`
- `find()`
- `count()`
- `reverse()`
- `join()`
- `partition()`
- `rsplit()`
- `map()`
- `filter()`
- `input()`
- `print()`
- `casefold()`
- `startswith()`

## loops

- `for`
- `while`

## real-word examples

- `enumerate() quiz generator`
- `len() input validation`
- `max() top performer`
- `min() slowest response`
- `range() pagination`
- `reversed() undo navigation`
- `round() currency formatting`
- `sum() total invoice calculation`
- `get() user profile lookup`
- `sorted() custom key`
- `append() build shopping cart`
- `extend() merge datasets`
- `index() error handling`
- `insert() insert priority task`
- `sort() leaderboard update`
- `split() csv line parser`
- `capitalize() format user input`
- `lower() normalize user input`
- `lstrip() clean user input`
- `removeprefix() clean log output`
- `removesuffix() trim file extension`
- `replace() clean CSV values`
- `rstrip() clean user-submitted data`
- `strip() clean user input`
- `title() format book titles`
- `upper() normalize codes`

## data types

- `list()`
- `tuple()`
- `str()`
- `dict()`
- `int()`
- `float()`

## standard library

- `itertools`
- `operator`
- `collections`
- `math`

## free cards

- `print()`done
- `len()` done
- `sum() — total of numbers in an iterable` done
- `max() — largest item` done
- `min() — smallest item` done
- `range() — generate a sequence of numbers` done
- `sorted() — return a sorted copy of an iterable` done
- `type() — check object type` done
- `input() — get user input`done
- `append() — add item to end of list` done
- `extend() — add all items from another iterable` done
- `pop() — remove and return item by index`
- `remove() — remove item by value`
- `sort() — sort list in place` done
- `split() — split string into list by delimiter` done
- `strip() — remove whitespace from both ends` done
- `replace() — replace substring with another` done
- `lower() — convert string to lowercase` done
- `upper() — convert string to uppercase` done
- `get() — safely retrieve value by key` done
